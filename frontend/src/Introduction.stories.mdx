import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="Introduction" />

# Welcome to the Firefox Relay Storybook

(If you're reading this `.mdx` file directly, run `npm run storybook` to view the storybook.)

Use the menu on the side to view previews of our components in various states.
This allows us to quickly verify what a component will look like, without having
to first create a user account that satisfies all requirements to a particular
rendering.

For example, here's an alias card:

<Story id="dashboard-aliases-alias--default" />
